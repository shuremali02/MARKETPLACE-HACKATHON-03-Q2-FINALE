/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["cdn.sanity.io", "i.pinimg.com", "res.cloudinary.com"]
    },
    reactStrictMode:false
};

export default nextConfig;
