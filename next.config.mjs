/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["cdn.sanity.io", "i.pinimg.com", "res.cloudinary.com","pin.it"]
    },
    reactStrictMode:false
};

export default nextConfig;
